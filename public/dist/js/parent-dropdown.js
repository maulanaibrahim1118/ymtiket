$(document).ready(function () {
    $("#role").select2({
        dropdownParent: $("#role").parent(), // Menentukan parent untuk dropdown
    });

    $("#position_id").select2({
        dropdownParent: $("#position_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#location").select2({
        dropdownParent: $("#location").parent(), // Menentukan parent untuk dropdown
    });

    $("#sub_division").select2({
        dropdownParent: $("#sub_division").parent(), // Menentukan parent untuk dropdown
    });

    $("#client_id").select2({
        dropdownParent: $("#client_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#ticket_for").select2({
        dropdownParent: $("#ticket_for").parent(), // Menentukan parent untuk dropdown
    });

    $("#asset_id").select2({
        dropdownParent: $("#asset_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#category_asset_id").select2({
        dropdownParent: $("#category_asset_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#wilayah").select2({
        dropdownParent: $("#wilayah").parent(), // Menentukan parent untuk dropdown
    });

    $("#category_ticket_id").select2({
        dropdownParent: $("#category_ticket_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#asset_change").select2({
        dropdownParent: $("#asset_change").parent(), // Menentukan parent untuk dropdown
    });

    $("#code_access").select2({
        dropdownParent: $("#code_access").parent(), // Menentukan parent untuk dropdown
    });

    $("#area_id").select2({
        dropdownParent: $("#area_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#regional_id").select2({
        dropdownParent: $("#regional_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#user_id").select2({
        dropdownParent: $("#user_id").parent(), // Menentukan parent untuk dropdown
    });

    $("#wil").select2({
        dropdownParent: $("#wil").parent(), // Menentukan parent untuk dropdown
    });

    $("#cat").select2({
        dropdownParent: $("#cat").parent(), // Menentukan parent untuk dropdown
    });

    $("#filter1").select2({
        dropdownParent: $("#filter1").parent(), // Menentukan parent untuk dropdown
    });

    $("#filter2").select2({
        dropdownParent: $("#filter2").parent(), // Menentukan parent untuk dropdown
    });

    $("#source").select2({
        dropdownParent: $("#source").parent(), // Menentukan parent untuk dropdown
    });
});
